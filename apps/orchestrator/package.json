{
  "name": "@pear/orchestrator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "tsc",
    "seed": "bun run seed/index.ts"
  },
  "dependencies": {
    "ioredis": "^5.3.2",
    "@pear/shared": "workspace:*",
    "@pear/db": "workspace:*",
    "@pear/logger": "workspace:*",
    "@pear/redis": "workspace:*"
  }
}
