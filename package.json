{
  "name": "pear-limit-engine",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "bunx turbo run dev --parallel",
    "build": "bunx turbo run build",
    "test": "bunx turbo run test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "turbo": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
